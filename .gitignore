node_modules/
data/
.env
credenciais/
*.log

# Pasta de documentos do sistema (vive no Google Drive; nunca versionar)
ARCHÉ/
ARCHE/

# Artefatos de migração (contêm dados pessoais)
estado-migrado.json
migracao-map.json
migrar-manus.mjs
backup-dossie-*.json
removidos-dossie-psicologia.json

# Pacote do registro no INPI: é grande e se reconstrói igual a partir do
# commit (scripts/pacote-inpi.mjs). O que se versiona é o HASH, não o pacote.
docs/inpi/arche-codigo-fonte-*.txt
